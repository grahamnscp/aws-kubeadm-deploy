#!/bin/bash

# subsitute private hostname in joinconfigmap
HOSTNAME=`hostname -f`
sed -i "s/##THIS_HOSTNAME##/${HOSTNAME}/g" /root/kubeadm-master-join-configmap.yaml

# Join the cluster
kubeadm join --config=/root/kubeadm-master-join-configmap.yaml
